{
  "name": "aws-clone",
  "version": "1.0.0",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && .\\venv\\Scripts\\activate && uvicorn main:app --reload",
    "dev": "start npm run dev:backend && npm run dev:frontend"
  }
}
